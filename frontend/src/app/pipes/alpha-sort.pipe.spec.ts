import { AlphaSortPipe } from './alpha-sort.pipe';

describe('AlphaSortPipe', () => {
  it('create an instance', () => {
    const pipe = new AlphaSortPipe();
    expect(pipe).toBeTruthy();
  });
});
